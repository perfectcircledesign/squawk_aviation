<% content_for :body_content_left do %>
  <ul id="employers">
    <% @employers.each do |employer| %>
      <li>
        <%= link_to employer.name, refinery.employers_employer_path(employer) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
